float battVoltage() {
  // read the input on analog pin A0:
//  int adcValue = analogRead(A3);
//  float pinVoltage = adcValue * (3.25 / 4095.0);  // 12 bits between GND and Vcc
//  float sourceVoltage = 1.996 * pinVoltage;
//  return(sourceVoltage);
}
